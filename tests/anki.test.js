const test = require("node:test"); const assert = require("node:assert/strict"); const a = require("../lib/anki.js");
test("builds API envelope", () => assert.deepEqual(a.requestBody("version"), { action: "version", version: 6, params: {} }));
test("parses Anki response", () => { assert.equal(a.parseResponse({ result: 6, error: null }), 6); assert.throws(() => a.parseResponse({ result: null, error: "bad" }), /bad/); });
test("composes one safe back field from all edited sections", () => { const n = a.buildNote({ deckName: "D", modelName: "Basic", fieldMap: { term: "Front", cardBack: "Back" }, values: { term: "<x>", partOfSpeech: "noun", meaning: "针；尖", videoSentence: "A <x>.", videoTranslation: "一根针。", generatedExample: "The needle is sharp.", exampleTranslation: "针很尖。", reference: "剧名 · 约 02:35" }, tags: ["one", "one"] }); assert.equal(n.fields.Front, "&lt;x&gt;"); assert.match(n.fields.Back, /noun；针；尖/); assert.match(n.fields.Back, /A &lt;x&gt;\.<br>译文：一根针。/); assert.match(n.fields.Back, /The needle is sharp\.<br>译文：针很尖。/); assert.match(n.fields.Back, /约 02:35/); assert.deepEqual(n.tags, ["one"]); });
test("rejects incomplete note configuration", () => { assert.throws(() => a.buildNote({ deckName: "", modelName: "Basic" }), /牌组/); assert.throws(() => a.buildNote({ deckName: "D", modelName: "Basic", fieldMap: { term: "Front", cardBack: "Front" }, values: { term: "x", meaning: "m" } }), /不同/); });
test("rejects malformed Anki envelope", () => assert.throws(() => a.parseResponse({ result: 1 }), /格式/));
test("revalidates note against live card template", () => { const note = { deckName: "D", modelName: "Basic", fields: { Front: "x", Back: "y" } }; const meta = { decks: ["D"], models: ["Basic"], fields: ["Front", "Back"], templates: { "Card 1": [["Front"], ["Back"]] } }; assert.equal(a.validateNoteAgainstMetadata(note, meta), note); assert.throws(() => a.validateNoteAgainstMetadata(note, { ...meta, templates: { "Card 1": [["Back"], ["Front"]] } }), /模板/); });
