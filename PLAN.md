# 暂停查词与 Anki 集成开发计划

> 状态：待审核，尚未开始实现  
> 计划分支：`codex/paused-dictionary-anki`（审核通过后从当前 `main` 创建）  
> 当前基线：`main` / `v2.0.4` / `01c4101`  
> 约束：两个新功能只在视频暂停时可交互；播放期间不得增加可感知的字幕翻译延迟或阻挡播放器操作。

## 1. 当前项目分析

### 1.1 项目形态与运行方式

- 这是一个无构建步骤、无第三方运行时依赖的 Manifest V3 Edge/Chrome 扩展。
- 支持页面为 Netflix `/watch/<id>`、YouTube `/watch` 和 `/shorts/`。
- `content.js` 读取页面中已经渲染的字幕，将请求发给后台 service worker；`background.js` 再访问用户本机的 OpenAI 兼容服务（默认 `http://127.0.0.1:8080`）。
- 设置保存在 `chrome.storage.sync`。当前权限只有 `storage`，网络白名单为 Netflix、YouTube、`127.0.0.1` 和 `localhost`。
- 项目没有打包器、测试框架、lint 配置或 CI；代码直接作为扩展脚本加载。

### 1.2 现有文件职责

| 文件 | 当前职责 | 与新功能的关系 |
| --- | --- | --- |
| `manifest.json` | MV3 配置、页面注入、权限和本机服务访问范围 | 需要允许 AnkiConnect 的本机端口；登记新增脚本（如采用拆分脚本） |
| `content.js` | 页面识别、字幕采集、翻译排队/取消、字幕层创建与定位 | 新功能的主要入口：检测实际视频、暂停态、选词、词典面板和卡片编辑器 |
| `content.css` | 播放页字幕覆盖层样式 | 需要增加暂停态可选文本、词典卡片、编辑器、状态提示和小屏适配 |
| `background.js` | 翻译请求、超时/重试、模型发现、服务检测、消息分发 | 需要新增查词与 AnkiConnect 消息协议，并继续把跨域/本机请求集中在后台 |
| `options.html` / `options.js` | 翻译、字幕样式和本机服务设置 | 需要增加查词开关、AnkiConnect 地址、牌组/笔记类型配置和连接测试 |
| `popup.html` / `popup.js` | 总开关、翻译服务在线状态、打开设置 | 首版仅补充暂停查词提示；不把复杂 Anki 设置塞进弹窗 |
| `i18n.js` | 设置页和弹窗的中英文文本替换 | 需要覆盖所有新增设置文案；播放页动态文案需使用独立消息表 |
| `_locales/zh_CN/messages.json` | 扩展名称和描述 | 如产品描述包含新能力则更新 |
| `ui.css` | 设置页和弹窗共用样式 | 需要增加 Anki 设置区、测试状态和禁用态样式 |
| `README.md` / `README.en.md` | 安装、设置、隐私说明 | 需要补充暂停查词、AnkiConnect 安装配置、数据流和故障排查 |

### 1.3 现有关键调用链

1. `content.js:init()` 创建 `#nf-zh-overlay`，监听 DOM 变化并每 500 ms 扫描字幕。
2. `scanSubtitles()` 更新原文/译文状态，经 80 ms 防抖后发送 `TRANSLATE`。
3. `background.js` 发现本地模型并调用 `/v1/chat/completions`，带超时、重试与取消。
4. `content.js` 用版本号避免旧响应覆盖新字幕，并把最近结果放入内存缓存。
5. `positionOverlay()` 分别处理 YouTube 与 Netflix 的字幕位置。

### 1.4 对新功能有直接影响的现状

- 覆盖层目前是 `pointer-events: none`，原文行始终带 `nf-zh-hidden`，所以现在无法选择单词。
- 当前代码没有持有 `<video>` 引用，也没有播放/暂停状态机；Netflix/YouTube 的 SPA 导航和播放器重建需要动态重新绑定。
- 字幕文本会被播放器频繁重建；交互层不能直接依赖站点原字幕 DOM，否则选区很容易丢失。应使用扩展自己的 `sourceLine` 作为稳定的选词表面。
- 翻译服务只有自由文本解析器。查词需要单独的提示词、严格 JSON 解析、输入长度限制和独立缓存，不能复用字幕翻译的短输出逻辑。
- Anki 没有浏览器原生 API；可行的本地集成方式是 Anki Desktop + AnkiConnect（默认 `http://127.0.0.1:8765`）。牌组和笔记类型因用户环境而异，必须可配置、可测试并处理字段不匹配。
- `chrome.storage.sync` 有配额限制。只存配置，不存查词历史、完整字幕或卡片内容。

## 2. 首版范围与产品决策

### 2.1 首版包含

- 仅当当前页面的主视频确实处于 `paused` 且尚未结束时，显示并启用可选中的原字幕。
- 用户用鼠标选中原字幕中的一个词或短语后，打开轻量词典面板。
- 词典首版复用现有本地 OpenAI 兼容服务，返回词形、读音（模型能可靠给出时）、词性、简明释义、当前字幕语境中的释义，以及一个由 AI 新生成的自然例句。
- 查词请求只在用户完成选择后发出；播放期间不监听高频选区、不查词、不展示交互面板。
- 词典卡片只展示一个“添加到 Anki”按钮；只有用户点击该按钮后才打开轻量添加页面，用户可编辑词语、释义、视频原句、AI 新例句、标签和目标牌组，确认后才写入 Anki。
- Anki 通过 AnkiConnect 本机 HTTP API 接入；提供连接测试、牌组刷新、笔记类型刷新、字段映射和重复卡片提示。
- 中英文扩展 UI、错误信息、使用文档和隐私说明同步更新。

### 2.2 首版明确不包含

- 不下载或内置大型离线词典数据包，不调用公网词典 API。
- 不做自动分词、自动查词、播放时悬停查词、查词历史或批量制卡。
- 不自动安装/启动 Anki 或 AnkiConnect，不修改 AnkiConnect 配置。
- 不保存整段观看历史，不向开发者服务器发送字幕、查词或卡片内容。
- 不保证模型生成的音标/词性等同于权威辞书；界面与文档会明确“本地 AI 辅助释义”。后续可在统一 provider 接口下增加真正的离线词典。

### 2.3 默认卡片数据

- `Front`：所选词或短语。
- `Back`：简明释义，可在提交前编辑。
- `Video sentence`：包含所选词的当前视频原字幕，保留真实语境。
- `Generated example`：本地 AI 生成的另一个自然、简短例句，避免与视频原句重复；同时生成与目标语言一致的例句释义时，可映射到独立字段或合并进背面。
- 可选字段：当前译文、AI 例句译文、来源页面标题和 URL；仅当所选笔记类型存在对应字段并完成映射时写入。
- 标签：默认 `subtitle-learning`，允许在编辑器中修改。
- 卡片提交必须由用户点击确认，绝不因查词成功而自动写入 Anki。

### 2.4 牌组策略

- 设置页要求用户选择一个**默认牌组**，作为日常快速添加的目标；不会强制每部影片使用单独牌组。
- 点击“添加到 Anki”后，添加页面显示牌组下拉框，默认选中上述牌组；用户可为当前卡片临时切换到任意已有牌组。
- 添加页面提供“新建牌组”入口。用户明确输入并确认名称后，通过 AnkiConnect `createDeck` 创建，再将当前卡片加入其中；扩展不会仅因打开一部影片而自动创建牌组。
- 推荐的可选命名方式是 Anki 子牌组，例如 `Subtitle Learning::影片名`。这适合希望按影片单独复习的用户，但不是默认行为，以免长期产生大量牌组。
- 无论使用统一牌组还是影片牌组，默认附加经过清理的来源标签，例如 `source-youtube`、`title-<slug>`。因此更推荐“一个长期牌组 + 按来源标签筛选”；用户仍可按个人习惯切换为“一部影片一个子牌组”。
- 首版不自动记忆“影片 → 牌组”映射。添加页面在同一播放页会记住本次会话最后选择的牌组，刷新页面后回到全局默认牌组，避免把观看记录持久化。

## 3. 目标架构

为保持无构建依赖并提高可测试性，新增逻辑按“纯逻辑 + 浏览器适配”拆分：

- `content.js`：只负责编排播放器状态、字幕状态、选区和视图生命周期。
- `content.css`：只负责暂停态交互面板表现。
- `background.js`：只负责消息路由、请求取消和错误归一化。
- `lib/dictionary.js`（新增）：查词输入校验、提示词构造、响应解析和结果标准化。
- `lib/anki.js`（新增）：AnkiConnect 请求封装、连接检查、牌组/模型/字段读取、卡片 payload 构造和错误归一化。
- `lib/shared.js`（新增，可在实现时按实际复用量决定是否保留）：本机 URL 校验、边界裁剪和可测试的通用纯函数。
- `tests/*.test.js`（新增）：使用 Node 内置 `node:test`，避免新增 npm 依赖；测试纯函数和协议 payload。

后台 service worker 可用 `importScripts()` 加载无模块依赖的库；若浏览器兼容验证显示不稳定，则把后台改为 MV3 module service worker，并在独立步骤验证 Edge/Chrome。内容脚本使用 `manifest.json` 中的有序 `js` 数组加载共享逻辑，不引入打包器。

## 4. 分步实施计划

### 阶段 0：建立隔离开发基线

- [x] **0.1 创建功能分支并记录基线**
  - 修改文件：无。
  - 操作：从审核通过时的 `main` 创建 `codex/paused-dictionary-anki`；确认工作区仅有获批的 `PLAN.md`。
  - 预期结果：所有后续实现与现有稳定版本隔离。
  - 验收标准：`git status` 显示正确分支；基线扩展可在 Edge/Chrome 重新加载；Netflix/YouTube 原有翻译行为无回归。

- [x] **0.2 建立最小自动化测试骨架**
  - 修改文件：新增 `tests/dictionary.test.js`、`tests/anki.test.js`、必要时新增 `package.json`（只提供 `test` 脚本，不引入第三方包）。
  - 预期结果：可通过一个命令运行 Node 内置测试；先覆盖输入清理、本机 URL 验证和 Anki payload 的契约样例。
  - 验收标准：测试命令退出码为 0；测试不依赖 Chrome、Anki、网络或本地模型。

### 阶段 1：播放器暂停状态与交互边界

- [x] **1.1 实现主视频发现和可恢复的事件绑定**
  - 修改文件：`content.js`，测试需要时新增 `lib/shared.js`、`tests/player-state.test.js`。
  - 实现要点：在页面多个 `<video>` 中选择可见面积最大的主视频；监听 `play`、`pause`、`ended`、`emptied`；DOM/SPA 变化后解除旧监听并绑定新节点；页面隐藏或找不到视频时退出交互态。
  - 预期结果：YouTube 普通页、Shorts 和 Netflix 播放器重建后仍能得到准确状态。
  - 验收标准：暂停后进入交互态，继续播放后在一个事件循环内退出；切换视频/剧集不残留旧监听；播放期间原翻译请求数量和扫描频率不增加。

- [x] **1.2 增加暂停态可选原文层**
  - 修改文件：`content.js`、`content.css`。
  - 实现要点：暂停时显示扩展自己的原文行并仅对该行开启 `pointer-events`、`user-select`；播放时恢复隐藏和穿透；不直接修改 Netflix/YouTube 原字幕 DOM。
  - 预期结果：暂停后用户能选择当前原字幕，播放器其他区域仍可点击。
  - 验收标准：播放时点击/快捷键/控制条不受影响；暂停时可选择文字；恢复播放立即清除浏览器选区并关闭词典/编辑器；全屏、小窗和窄播放器不遮挡关键控制条。

- [x] **1.3 实现安全的选区提取**
  - 修改文件：`content.js`、`lib/shared.js`、`tests/selection.test.js`。
  - 实现要点：只接受锚点和焦点都位于扩展原文行内的选区；合并空白、裁掉边缘标点；限制长度（建议 1–80 个字符）；忽略空选区、整句误选和面板内部选择；支持 CJK 无空格文本和带撇号/连字符的拉丁词。
  - 预期结果：一次有效 `mouseup` 只产生一次查词意图。
  - 验收标准：中/英/日/韩典型样例单测通过；跨节点、反向选择、快速重复选择不会抛错或重复请求。

### 阶段 2：轻量词典功能

- [x] **2.1 定义查词消息协议和结果模型**
  - 修改文件：新增 `lib/dictionary.js`、修改 `background.js`、`manifest.json`。
  - 协议建议：`LOOKUP_WORD` 请求包含 `requestId`、`term`、`sentence`、源/目标语言；响应固定为 `{ term, normalizedTerm, pronunciation, partOfSpeech, definitions, contextualMeaning, generatedExample, generatedExampleTranslation }`。视频原句直接使用请求时的可信快照，不要求模型复述。
  - 预期结果：词典响应与字幕翻译响应互不影响，能独立取消、超时、报错和缓存。
  - 验收标准：非法输入在发送网络请求前被拒绝；未知字段被忽略；缺失字段使用空值而非导致界面崩溃。

- [x] **2.2 实现本地 AI 查词请求与健壮解析**
  - 修改文件：`background.js`、`lib/dictionary.js`、`tests/dictionary.test.js`。
  - 实现要点：复用模型发现和本机服务地址；使用专用 system prompt 与低温度；要求纯 JSON；去除代码围栏/思考标签；校验数组长度和字段长度；查词缓存键包含服务、语言、词和语境；限制缓存大小；新查词取消旧查词。
  - 预期结果：本地服务在线时得到紧凑、稳定的结构化释义；模型输出不规范时给出可理解错误。
  - 验收标准：正常 JSON、代码围栏 JSON、多余文本、字段缺失、无效 JSON、超时和取消等单测通过；不改动现有 `TRANSLATE` 行为。

- [x] **2.3 构建词典浮层及生命周期**
  - 修改文件：`content.js`、`content.css`。
  - 实现要点：浮层锚定选区但限制在视口/播放器范围；包含加载、成功、空结果、错误、重试和关闭状态；使用 DOM API 与 `textContent` 渲染模型输出，禁止拼接不可信 HTML；提供“加入 Anki”入口。
  - 预期结果：暂停选词后可快速阅读释义，界面轻量且不破坏字幕定位。
  - 验收标准：点击外部、按 Escape、恢复播放、字幕变化或播放器销毁时正确关闭；慢响应不会覆盖后一次选择；所有模型文本按纯文本显示，无 HTML 注入。

- [x] **2.4 增加词典设置和动态文案**
  - 修改文件：`options.html`、`options.js`、`ui.css`、`i18n.js`、`content.js`。
  - 实现要点：增加“暂停查词”总开关（默认开启）和简短行为说明；播放页面板使用中英文消息映射，不依赖设置页的 DOM walker。
  - 预期结果：用户可以关闭新交互而保留实时字幕翻译。
  - 验收标准：设置保存后已打开页面即时生效；中英文新增文案完整；关闭后无选区监听触发的请求。

### 阶段 3：AnkiConnect 基础接入

- [x] **3.1 实现 AnkiConnect 客户端与安全边界**
  - 修改文件：新增 `lib/anki.js`、修改 `background.js`、`manifest.json`、新增/扩展 `tests/anki.test.js`。
  - 实现要点：默认地址 `http://127.0.0.1:8765`；只接受 HTTP loopback 主机（`127.0.0.1`、`localhost`、`::1`）和有效端口；实现 `version`、`deckNames`、`createDeck`、`modelNames`、`modelFieldNames`、`canAddNotes`、`addNote`；校验 AnkiConnect `{result,error}` 包装。
  - 预期结果：所有 Anki 网络访问由后台完成，不开放任意远程 URL。
  - 验收标准：协议成功、Anki 未启动、插件未安装、HTTP 错误、协议 error、返回格式错误和超时均有单测或可复现检查；现有 localhost 翻译权限保持有效。

- [x] **3.2 增加 Anki 设置、连接测试和元数据刷新**
  - 修改文件：`options.html`、`options.js`、`ui.css`、`i18n.js`。
  - 设置项：启用 Anki、AnkiConnect 地址、默认牌组、笔记类型、字段映射（词语/释义为必需，视频原句/AI 新例句/例句译文/来源为可选）、默认标签。
  - 实现要点：先测试连接，再拉取牌组和笔记类型；笔记类型变化后刷新字段；保存前验证必填映射不重复且字段确实存在；元数据只用于选择，不持久化大量列表。
  - 预期结果：不同 Anki 用户可把数据映射到自己的模型，不假定一定存在 `Basic`、`Front`、`Back`。
  - 验收标准：连接正常时可选择真实牌组/模型/字段；连接失败时不丢失已保存配置并给出安装/启动提示；禁用 Anki 时相关输入不可编辑且查词仍可用。

- [x] **3.3 增加权限与 AnkiConnect CORS 配置说明**
  - 修改文件：`README.md`、`README.en.md`，必要时 `options.html`。
  - 实现要点：说明安装 AnkiConnect、保持 Anki Desktop 运行、允许扩展来源的配置方式；记录默认端口与最小暴露原则。
  - 预期结果：用户无需猜测“连接失败”的原因。
  - 验收标准：在全新 Anki 环境按文档完成配置后，设置页连接测试成功；文档不建议将 AnkiConnect 暴露到局域网或公网。

### 阶段 4：卡片编辑与写入

- [x] **4.1 构建轻量卡片编辑器**
  - 修改文件：`content.js`、`content.css`、`i18n.js`（或播放页消息表）。
  - 实现要点：词典卡片本身只提供一个“添加到 Anki”按钮；点击后才打开添加页面。从词典结果预填词语、释义、视频原句、AI 新例句、例句译文和标签；牌组下拉框默认选择全局默认牌组，但允许本次切换或经二次确认新建牌组；只展示已映射字段；支持编辑、取消、保存中、成功和失败状态；保持焦点管理和 Escape 行为清晰。
  - 预期结果：用户在写入前能审阅并轻量修改卡片内容。
  - 验收标准：未点击 Anki 按钮时不创建编辑器或访问 Anki；输入内容不会因背景字幕扫描被刷新；恢复播放时编辑器关闭但不提交；Tab 顺序可用；重复点击提交不会创建多张卡；新牌组只在用户明确确认后创建。

- [x] **4.2 实现预检查、重复处理和提交**
  - 修改文件：`content.js`、`background.js`、`lib/anki.js`、`tests/anki.test.js`。
  - 实现要点：后台重新校验 deck/model/fields；先调用 `canAddNotes`；使用 `duplicateScope: "deck"`（如 AnkiConnect 版本支持）或采用默认重复规则；通过一次性的提交 token/按钮锁避免双写；将页面 URL 作为纯文本来源字段而非 HTML。
  - 预期结果：成功返回 note id；重复卡、字段错误和 Anki 离线均显示明确结果，用户编辑内容不丢失。
  - 验收标准：真实 Anki 中卡片进入指定牌组、字段和标签正确；重复提交不会静默生成重复卡；失败后可修改并重试。

- [x] **4.3 隐私与内容安全检查**
  - 修改文件：相关实现文件、`README.md`、`README.en.md`。
  - 实现要点：模型输出和用户输入全部用纯文本 DOM；禁止任意 URL 请求；日志不输出完整字幕/卡片；仅用户确认后向 Anki 发送；不向同步存储写入学习内容。
  - 预期结果：新功能维持项目“数据只在本机流转”的定位。
  - 验收标准：代码审查找不到 `innerHTML` 注入不可信内容、远程词典域名或卡片正文持久化；文档准确描述本地 AI 与 AnkiConnect 两条数据流。

### 阶段 5：回归、兼容性与发布准备

- [ ] **5.1 自动化测试与静态检查**
  - 修改文件：测试文件；只在发现问题时修改实现。
  - 预期结果：纯逻辑、消息 payload、错误分支和边界输入具备回归保护。
  - 验收标准：全部 Node 测试通过；所有 JS 文件语法检查通过；`manifest.json` 可解析；无新增第三方依赖漏洞面。

- [ ] **5.2 Chrome/Edge 手工功能矩阵**
  - 修改文件：必要时在 `PLAN.md` 下方记录验证结论，问题修复进入对应实现文件。
  - 验证场景：YouTube 普通/全屏/影院/迷你播放器/Shorts，Netflix 普通/全屏；中文、英文、日文和韩文字幕；暂停、恢复、拖动进度、切换视频、切换字幕、SPA 导航、扩展热更新。
  - 验收标准：仅暂停时可选择；播放时无面板且点击穿透；字幕翻译延迟与基线无可感知差异；所有关闭/取消路径无残留 UI 或旧响应覆盖。

- [ ] **5.3 Anki 真实环境验收矩阵**
  - 修改文件：只在发现问题时修改实现或文档。
  - 验证场景：Anki 未启动、未安装 AnkiConnect、默认 Basic、自定义模型、自定义字段、牌组不存在/被重命名、重复卡、非 ASCII 内容、很长释义、连接中断后恢复。
  - 验收标准：成功路径数据准确；失败路径不误报成功、不丢编辑内容、不生成部分卡片；错误提示能够指导用户恢复。

- [ ] **5.4 文档、版本和最终变更审查**
  - 修改文件：`README.md`、`README.en.md`、`manifest.json`、`_locales/zh_CN/messages.json`；版本号在功能完全验收后再确定。
  - 预期结果：安装、使用、权限、隐私、限制和排错文档与实际行为一致。
  - 验收标准：新用户按文档能完成暂停查词和制卡；中英文说明一致；最终 diff 不包含压缩包、凭据、用户卡片数据或无关文件。

## 5. 任务依赖关系

```text
阶段 0 测试基线
  └─ 阶段 1 暂停状态 + 可选字幕
       └─ 阶段 2 词典协议 + 词典 UI
            ├─ 阶段 3 AnkiConnect 客户端 + 设置
            │    └─ 阶段 4 卡片编辑 + 写入
            └──────────────────────────┘
                         └─ 阶段 5 全量回归与文档
```

- 1.1–1.3 必须先完成，因为“只在暂停时触发”是两项功能共同的安全边界。
- 2.1–2.2 可与 2.3 的静态 UI 骨架分开测试，但接入验收需要三者全部完成。
- 3.1 的客户端可独立于页面 UI 测试；3.2 依赖 3.1 提供牌组/模型/字段元数据。
- 4.1 依赖稳定的词典结果模型；4.2 同时依赖 3.1/3.2 的有效 Anki 配置。
- 阶段 5 必须在所有功能完成后执行；每个阶段内部也要运行已有测试，不能把回归集中到最后。

## 6. 主要风险与缓解措施

| 风险 | 影响 | 缓解方案 | 验证点 |
| --- | --- | --- | --- |
| Netflix/YouTube 经常重建播放器 DOM | 暂停监听丢失或绑定旧视频 | 每次扫描校验主视频身份；显式解绑旧事件；SPA 切换纳入手测矩阵 | 切剧集/切视频/Shorts 连续滑动 |
| 暂停后站点原字幕可能隐藏 | 用户看不到可选文本 | 选词基于扩展已缓存的 `sourceLine`，不依赖暂停后的站点字幕可见性 | 暂停瞬间和字幕消失后仍可选当前缓存行 |
| 覆盖层抢占播放器点击 | 无法继续播放或操作控制条 | 仅原文行和面板在暂停态开启指针；根层仍尽量穿透；播放事件强制清理 | 点击视频恢复、控制条、全屏退出 |
| 字幕更新导致选区/面板跳动 | 查词结果对应错误文本 | 查词请求携带选中词和句子快照；请求版本隔离；交互期间不改写编辑器 | 拖进度、慢请求、连续选词 |
| 本地翻译模型不擅长结构化 JSON | 词典解析失败或释义不稳定 | 严格提示、容错清理、schema 校验、字段裁剪、可重试错误；保留 provider 边界 | 多模型与畸形响应测试 |
| “AI 释义”不等同权威辞书 | 用户对准确性预期过高 | UI/文档明确标注本地 AI 辅助；不伪造来源/音标；后续可换离线词典 provider | 文案审查和空字段行为 |
| AnkiConnect 未安装、未运行或 CORS 未配置 | 无法连接 | 设置页连接测试、逐类错误提示、中英文安装指南 | 全新 Anki 环境验收 |
| 用户的牌组/模型/字段高度自定义 | 写入失败或字段错位 | 动态拉取元数据并显式映射；设置默认牌组、添加时允许覆盖；保存与提交时双重校验 | 自定义模型、字段重命名、临时切换/新建牌组 |
| 每部影片自动建牌组造成牌组膨胀 | Anki 导航和复习配置变复杂 | 默认使用一个长期牌组并以标签记录来源；仅在用户明确确认时创建影片子牌组 | 多影片连续添加、刷新后的默认行为 |
| 双击/重复提交生成多卡 | 数据污染 | `canAddNotes` 预检查、提交锁、单次 token、明确重复提示 | 快速双击和网络慢速场景 |
| MV3 service worker 会休眠 | 内存缓存/请求状态丢失 | 缓存只作为优化；每个请求自包含；不依赖后台长期状态保证正确性 | service worker 重启后查词/制卡 |
| `chrome.storage.sync` 配额 | 设置保存失败 | 仅保存小型配置和映射，不保存查词/卡片/元数据列表 | 存储内容审查 |
| 新 localhost 权限扩大攻击面 | 可访问用户本机服务 | 地址严格限制 loopback；固定 API action 白名单；不接受页面提供的地址 | URL 校验单测和安全审查 |

## 7. 阶段性完成定义

- [ ] 原有实时翻译在 Netflix/YouTube 上无功能和性能回归。
- [ ] 播放时所有新交互完全不可用、不可见且不阻挡播放器。
- [ ] 暂停时可选择原字幕，并得到来自既有本地 AI 服务的轻量释义。
- [ ] 用户可连接自己的 AnkiConnect、选择牌组和字段映射、编辑并确认写入一张卡片。
- [ ] 所有网络目标均限制为用户配置的本机翻译服务和本机 AnkiConnect。
- [ ] 自动化测试、Chrome/Edge 手测矩阵、Anki 真实环境矩阵全部通过。
- [ ] 中英文设置、错误提示、README、隐私说明与最终行为一致。

## 8. 审核时需要确认的默认方案

以下方案已作为可落地的首版默认值写入计划；若需调整，应在开始实现前确认：

- [ ] 接受“轻量辞书”首版使用现有本地 AI 服务生成结构化释义，而不是下载词典或访问公网 API。
- [ ] 接受只对“原字幕”开放选词；译文暂不作为查词入口。
- [ ] 接受 Anki 集成依赖用户自行安装并运行 Anki Desktop + AnkiConnect。
- [ ] 接受卡片字段采用动态映射，而不是强制创建一个固定的自定义笔记类型。
- [ ] 接受“一个默认牌组 + 添加页面临时切换/手动新建牌组 + 来源标签”的牌组策略，不自动为每部影片创建牌组。
- [ ] 接受播放恢复时直接关闭尚未提交的词典面板/编辑器，以严格满足“只在暂停时触发”。
